#include <bits/stdc++.h>
using namespace std;

int allocateBooks(vector<int> arr, int n, int m){
    int start = 0;
    int sum = 0;

    for(int i = 0; i < n; i++){
        sum += arr[i];
    }
    
}